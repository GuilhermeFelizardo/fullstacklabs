name: CI Pipeline
on:
  pull_request:
    branches:
      - devel
      - stage
      - prod

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install dependencies
      run: npm install

    - name: Run ESLint
      run: npm run lint

    - name: Run Prettier
      run: npm run prettier

    - name: Run Jest Tests
      env:
        CI: true
      run: npm run test

    - name: Build the project
      run: npm run build
